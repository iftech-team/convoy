{"default":{"identifier":"default","description":"What the window is allowed to ask the backend for. Deliberately narrow: the commands this app defines, plus the event channel the terminal streams over, a native folder picker, desktop notifications, and writing a text file the user has just chosen in a save dialog. No shell, no HTTP, and no filesystem beyond that one picked path — everything that touches the machine goes through a command whose rules live in convoy-core.","local":true,"windows":["main"],"permissions":["core:default","opener:default","dialog:default","notification:default","fs:allow-write-text-file"]}}